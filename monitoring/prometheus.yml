global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: "mlops-api"
    metrics_path: "/metrics"
    static_configs:
      - targets:
          - "mlops-api:8000"
          - "host.docker.internal:8000"
